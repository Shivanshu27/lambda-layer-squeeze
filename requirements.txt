# Production dependencies for computer vision & lightweight inference
# Note: PyTorch CPU wheels are installed directly from the PyTorch index in the Dockerfile
# to prevent global index leakage and forced compilation of binary wheels from source.
pillow>=10.2.0
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.9.0.80
