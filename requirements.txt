# Production computer vision & lightweight ML inference dependencies
# Pinned to tested binary wheel releases to ensure reproducible, zero-compilation builds
opencv-python-headless==4.9.0.80
pillow>=10.2.0
numpy==1.26.4
onnxruntime==1.16.3

# Transitive and build dependencies commonly leaked into unoptimized layer builds
# (Targeted and stripped during dependency surgery)
boto3>=1.34.0
botocore>=1.34.0
pytest>=8.0.0
pre-commit>=3.6.0
sphinx-rtd-theme>=2.0.0
